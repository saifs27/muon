## Serval

Serval is an LLM inference implementation written in C++

## Build Instructions

To build, Serval requires a C++23 compatible compiler and CMake 3.31.

```
cmake -S. -Bbuild -DCMAKE_BUILD_TYPE=Release
cmake --build build
```
